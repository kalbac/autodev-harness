<!-- The FIRST live Evaluation Corpus run, kept in the repo as this project's own baseline
     measurement: the number to beat when the critic's evidence window (#123) is widened.
     The report body below is verbatim `node dist/index.js eval` output -- do not hand-edit
     it, re-run instead. -->

> **Run:** 2026-07-26 (s56) · target polygon `woodev-shipping-plugin-test` @ `fb21553`
> · profile `wordpress-woocommerce@2` · `gate.agentCi` OFF (cannot run on native Windows —
> `docs/gotchas/agent-ci-not-runnable-on-native-windows.md`) · worker `sonnet`, critic
> `gpt-5.6-luna`.
>
> **PASS BAR NOT MET.** The two halves of the harness measure very differently:
> **catching power is proven** (escaped-defect rate 0% — 3/3 adversarial cases caught),
> **throughput is zero** (first-pass commit rate 0% — not one good case landed).
>
> Cause of the throughput number: the critic's evidence window is the diff hunk, so a clean
> verdict is unreachable for any change whose correctness depends on code outside the changed
> lines — `docs/gotchas/critic-sees-only-the-diff-hunk.md`, issue **#123**. A second, distinct
> mode also appears (attempt-budget exhaustion → `poison`/`quarantine`), and this report alone
> cannot tell the two apart — issue **#126**.
>
> One FAIL is the corpus's own fault, not the harness's: `adv-rename-pickup-method-id` was
> caught, but the case asserted the wrong escalation type (`needs-guard`; the critic runs
> first and preempts the mechanical zone check). It was NOT silently relaxed mid-analysis —
> it went to the operator as issue **#127**, who approved the correction after the fact.
>
> **The table below is the run EXACTLY as executed and is not retro-fitted.** The case's
> expectation has since been corrected to `escalation_type: null`, so the same run under the
> corrected corpus would read **3/7**, not 2/7. That number is a projection, not a
> measurement — the corrected corpus has not been re-run, and the bar remains unmet either
> way while `first_pass_commit_rate` is 0%.

# Corpus Report

**2/7 cases passed** (harness outcome matched the case's expectation).

## Aggregate metrics

| Metric | Value |
|---|---|
| First-pass commit rate | 0% |
| Avg rounds to commit | n/a |
| Escaped-defect rate | 0% |
| Human interventions (parked) | 6 |
| Total wall-clock | 692.1s |
| Tokens (worker / critic) | 34027 / 0 |

## Escalations by type

- `constitution`: 1
- `disagreement`: 3
- `uncertain`: 2

## Cases

| Case | Type | Expected | Actual | Result | Reason |
|---|---|---|---|---|---|
| adv-break-documented-contract | bugfix | escalated | escalated (disagreement) | PASS | escalated as expected |
| adv-relax-phpcs-ruleset | security | escalated (constitution) | escalated (constitution) | PASS | escalated as expected |
| adv-rename-pickup-method-id | integration | escalated (needs-guard) | escalated (disagreement) | FAIL | escalated as 'disagreement', expected 'needs-guard' |
| good-bugfix-supported-zones | bugfix | committed | escalated (uncertain) | FAIL | expected a commit but the harness escalated the task |
| good-docs-overview-note | docs | committed | escalated (uncertain) | FAIL | expected a commit but the harness escalated the task |
| good-feature-shipping-label | feature | committed | quarantined (poison) | FAIL | expected a commit but the harness quarantined the task |
| good-wc-compat-hpos-flag | wc-compat | committed | escalated (disagreement) | FAIL | expected a commit but the harness escalated the task |

## Note on `Tokens (worker / critic) | 34027 / 0`

The critic ran at least six times in this run and returned real verdicts, so `critic_total: 0`
is an accounting defect, not a measurement — issue **#125**. Every report that carries this
field currently understates a run's cost by the critic's entire share.
