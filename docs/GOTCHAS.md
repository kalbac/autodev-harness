# GOTCHAS — Autodev Harness

> Index of mistakes-to-avoid. Each entry → atomic detail file in `gotchas/{slug}.md`.
> Scan the relevant tags before starting related work.
> Count: 70.

| Tag | Gotcha | Detail |
|-----|--------|--------|
| `[ops/daemon-run]` | Run the daemon with the **`serve`** verb (`node dist/index.js serve` → binds :4319 + serves `dist/ui` at `/`); the BARE binary defaults to conductor `run`, which dies `refusing to run on branch 'main'`. The conductor only runs when the project repo is on an `^autodev/` branch (guard `conductor.ts:517`, default pattern `schema.ts:45`) — New Project scaffold doesn't switch to it (s30 fix), so the first run trips the guard; and enqueue happens before the guard → orphaned PENDING tasks. | `gotchas/daemon-serve-verb-and-autodev-branch-guard.md` |
| `[ui/shadcn-zinc]` | zinc collapses surfaces: light `--card`==`--background`==white (& `--popover`); dark `--sidebar`==`--card`. So `bg-card` over the page needs a border, and a `bg-sidebar` rail won't separate from `bg-card` cards in dark — use `bg-muted` for tinted panels (page<muted<card reads in both themes). `text-white` hovers break in light (use `text-foreground`). Alias layers must NOT reuse shadcn names `muted`/`accent` (hijacks primitives); a class-only sed misses inline `var(--color-*)`. | `gotchas/shadcn-zinc-token-pitfalls.md` |
| `[ts/test-hang]` | An unterminated async loop with no-op (microtask) deps starves vitest's `setTimeout` timeout → the run HANGS uncatchably (process-killed at 5 min, no per-test failure), not a timeout. Ensure `run()`-style tests terminate (`maxIterations`/advancing clock). Also: a new foreground shell command KILLS the running background one — don't `echo` while waiting. | `gotchas/vitest-microtask-starvation-hang.md` |
| `[ts/typecheck-scope]` | An emit-scoped `tsconfig` (`rootDir: src`, `include: ["src/**"]`) silently EXCLUDES the top-level `test/**` tree from `tsc` → `npm run typecheck` is vacuously green there (vitest runs but doesn't typecheck). Add a `noEmit` `tsconfig.typecheck.json` (`rootDir: "."`, include src+test). | `gotchas/tsconfig-typecheck-skips-test-dir.md` |
| `[api/413-teardown]` | Returning HTTP 413 for an oversized body by calling `req.destroy()` BEFORE the response flushes = client sees a socket reset, not 413. Stop appending + reject in the reader; write 413 + `connection: close` in the handler; destroy the socket on `res` `finish`. | `gotchas/http-413-destroy-before-flush-resets-client.md` |
| `[test/vacuous-assert]` | A test can be green while proving nothing: (1) driving BOTH arms of an OR so either could pass it, (2) asserting an always-present label (`stray:`/`forbidden:`) instead of the value. Isolate one cause per test; assert the specific value. | `gotchas/vacuous-assertions-and-or-arm-isolation.md` |
| `[conductor/wiring]` | s07 composition-root deferred limitations: `zonesTouchedInDiff` reads main-root INVARIANTS (worktree-only zone edits caught by constitution/dirty-file instead); gate command strings are whitespace-split, not quote-aware (no spaces/quotes in commands or test paths — backlog a shell runner); `index.ts` is untested glue by design. | `gotchas/conductor-wiring-deferred-limitations.md` |
| `[ts/fail-closed]` | A "never-throws"/best-effort module must guard its **catch-block** logger and `env()` calls too — a throwing `deps.log` inside a `catch` re-throws the fail-closed path. Use a `safeLog` wrapper; test a throwing logger + throwing primary dep together. | `gotchas/never-throws-catch-block-logging.md` |
| `[ts/zod]` | zod `.optional()` + `exactOptionalPropertyTypes` types a field `\| undefined` — incompatible with a hand-written `x?: T`; derive types via `z.infer`. Vitest doesn't typecheck, so run `npm run typecheck` after parallel subagents. | `gotchas/zod-optional-exactoptional-derive-types.md` |
| `[ao/ui]` | The "AO chat-scroll bug" is a phantom — AO has no chat UI (tmux terminal, `scrollback:0`). Nothing to fix. | `gotchas/ao-chat-scroll-bug-is-a-phantom.md` |
| `[donor/openhands]` | OpenHands' real agent code lives in `software-agent-sdk`, not the main repo — study the SDK clone. | `gotchas/openhands-real-code-is-in-software-agent-sdk.md` |
| `[critic/codex]` | `codex exec` sandbox can't spawn subprocesses on Windows (`CreateProcessAsUserW failed: 5`) — embed the diff inline in the prompt and it reviews fine. | `gotchas/codex-exec-windows-sandbox-review-inline-diff.md` |
| `[critic/codex]` | `critic-verdict.schema.json` is not copied to `dist/` by `tsc` — the critic's `--output-schema` path breaks from a compiled build (works from source). Dist-copy deferred to Task 29. | `gotchas/critic-schema-json-not-copied-to-dist.md` |
| `[node/stdin-epipe]` | Writing `child.stdin` in `runNative` with no `'error'` listener → a child that closes its read end fast (fast-exiting `git` etc.) makes `stdin.end()` raise an UNHANDLED EPIPE that CRASHES the run. Flaky (raced: only ubuntu/node20 in s08 CI). Fix = swallowing `'error'` handler; child stdout/stderr/exit captured separately. | `gotchas/child-stdin-epipe-unhandled.md` |
| `[conductor/worker-report]` | Per-task worktree (divergence #1): the worker writes `worker-report.md` into the worktree cwd → dirty-file fence flags it STRAY → every task ESCALATEs before the gate, and the conductor can't find the report in runtimeDir. Fix = `harvestWorkerReport` relocates it worktree→runtimeDir before status-read+fence; unlink stale dest first (retry/re-claim carry-over). | `gotchas/worker-report-harvest-worktree-fence.md` |
| `[node/win-cmd-spawn]` | `node:child_process.spawn("codex")` → ENOENT on Windows for a PATH command that's a `.cmd` shim (npm-global), and node22 blocks `.cmd` without a shell (CVE-2024-27980). `claude.exe` works, `codex.cmd` doesn't. Fix = spawn via `cross-spawn` (PATH+PATHEXT + cmd.exe w/ verbatim args); POSIX passthrough. | `gotchas/runnative-windows-cmd-shim-spawn.md` |
| `[conductor/real-repo-run]` | Running on a REAL repo surfaces 3 prereqs the fixture never did: (1) fresh worktree has NO gitignored deps (vendor/node_modules) → use a dependency-free gate (`php -l`) or provision deps; (2) main tree must be CLEAN or `mergeAfterGate` throws; (3) `.autodev/` must be git-excluded or its runtime churn dirties the tree → merge throws. Plus: branch must match `^autodev/`. | `gotchas/harness-on-real-repo-prerequisites.md` |
| `[orchestrator/forbidden-paths]` | An LLM decompose can emit `forbidden_paths` using gitignore-style `!` negation the harness glob matcher (`*`/`?`/`**` only) does NOT support, overlapping `file_set` → the dirty-file fence flags the task's OWN required file as forbidden → escalate before gate. `validateTaskSpec` accepted the impossible spec (no cross-field check). Fix = superRefine rejects `file_set`∩`forbidden_paths` overlap (reuses fence's exact `globMatch`) + decompose-prompt documents glob semantics. | `gotchas/orchestrator-forbidden-paths-overlap.md` |
| `[orchestrator/bg-spawn-killed]` | Running `orchestrate` as a BACKGROUND command gets killed during the nested `claude -p --model opus` decompose spawn (silent, stops at "decomposing intent"); foreground runs complete. Intermittent. Run live proofs in the FOREGROUND (surfaces real exit/stderr). Killed runs leave aurora clean (transactional enqueue-after-validate). | `gotchas/orchestrate-background-run-killed.md` |
| `[api/static-traversal]` | Static file serving with only a lexical `..`-guard + a final-component `O_NOFOLLOW`/`lstat` still lets an INTERMEDIATE symlink DIR (`uiDir/assets -> /outside`) escape — `O_NOFOLLOW` guards only the LAST segment. Fix = `realpath` containment vs a canonicalized `uiDir` (serve-static pattern); one `realpath`→`open` TOCTOU residual accepted (needs openat2, not portable in Node). Siblings: SPA-fallback must be cross-platform LEXICAL not errno (ENOTDIR/ENOENT differ by OS); a truncated file must not keep `application/json`. | `gotchas/static-file-serving-symlink-traversal.md` |
| `[config/zod-strict]` | A `z.object` STRIPS unknown keys by default — after a hard-cut of a config block (rename/remove), an OLD file using the removed key loads clean and silently reverts every field to defaults (no error). Bit the R3 `worker:`/`critic:`→`roles:` cut (aurora's real config). Fix = `.strict()` on the root schema (fail loud) + migrate every on-disk file. | `gotchas/zod-strip-unknown-keys-silent-config-revert.md` |
| `[ui/serve-uidir-reporoot]` | `serve` resolves the UI bundle at `<project repoRoot>/dist/ui` (repoRoot = git root of cwd), NOT relative to the harness install. So orchestrating an EXTERNAL project (e.g. aurora) has no bundle unless one is placed there — and dropping `dist/ui` into the project tree DIRTIES it → `mergeAfterGate` throws (`[conductor/real-repo-run]`). For the s14 live proof: copy `dist/ui` into the project + add `dist/` to `.git/info/exclude`. Also: run `serve` DETACHED (Start-Process), NOT a bash-background — else the browser-triggered `orchestrate`'s nested `opus` decompose is killed (`[orchestrator/bg-spawn-killed]` applies to `serve` too). | `gotchas/ui-serve-uidir-reporoot.md` |
| `[ui/verdict-not-persisted]` | The critic/gate verdict object (clean/broken/uncertain + confidence) is NOT written to a per-task runtime file — only a `digest.md` line + (on non-clean) the escalation body + `critic-feedback.md`. So the dashboard's "verdict first-class" is rich ONLY on escalation (via `GET /escalations/:id`); a CLEAN-committed task has no readable verdict artifact (just the digest line). To surface verdict+confidence for committed tasks, persist `critic-verdict.json` as a runtime file (backlog; touches the conductor → full gate). | `gotchas/ui-verdict-not-persisted.md` |
| `[worktree/win-junction-follow]` | `git worktree remove --force` (and `rm -rf`) FOLLOWS an NTFS junction on Windows and recursively deletes the junction's REAL target (verified 6/6). Deps-provisioning links dep dirs as junctions, so teardown could wipe the clone's real `vendor/`. Rule: link-only-remove EVERY top-level reparse point (non-recursive `unlink`→`rmdir` + verify-gone) BEFORE any recursive worktree removal; refuse to recurse (teardown early-return / stale-cleanup throw) if a link can't be confirmed gone. Provision entries restricted to a single top-level segment so the non-recursive scan is complete. Residual: nested FOREIGN junctions (pre-existing git behavior, not ours). | `gotchas/win-git-worktree-remove-follows-junction.md` |

| `[ts/shared-promise-reject]` | Caching an in-flight Promise for concurrent consumers: only the creator awaited in try/catch — a second caller's bare `await cached` rethrows the SHARED rejection (500 instead of the {error}→503 path). Every awaiter needs the creator's rejection handling + compare-before-evict on cleanup; test concurrent FAILURE, not just concurrent success. | `gotchas/shared-inflight-promise-rejection.md` |
| `[refactor/extraction-eagerness]` | A "pure mechanical extraction" that consolidates N identical call sites into one shared field makes construction EAGER for all consumers — `run` never built the orchestrator before, and `buildOrchestrator` throws for unregistered adapters, so previously-working configs broke. Consolidate throwing constructors into a memoized lazy getter; review the initialization graph, not just moved lines. | `gotchas/extraction-consolidation-changes-laziness.md` |
| `[multiproject/id-keyed-caches]` | Project ids are RE-BINDABLE (registry edit/re-register) — three separate id-keyed caches went stale independently (hub roots → wrong-repo orchestration; watchers → stale broadcasts under a reused id; lastError → moved project inherits old error). Every id-keyed cache entry must carry its binding (path) and validate on hit; long-lived callbacks need a fire-time identity guard. | `gotchas/id-keyed-caches-rebindable-ids.md` |
| `[ci/win-83-realpath]` | GitHub's `windows-latest` runner exposes `os.tmpdir()` as an 8.3 short path (`C:\Users\RUNNER~1\…`). `fs.promises.realpath`/`realpathSync.native` EXPAND it to the long form; plain `fs.realpathSync` (non-native) does NOT — so a test comparing a `realpathSync`-seeded path to a code-canonicalized (native) path is green locally, red on the runner. Also: a fn matching against a stored `realpath`'d path must canonicalize its INPUT identically (admin `isRegistered` bug: stored long, compared raw). Fix = `realpathSync.native` in tests; `isRegistered` realpaths its arg like `register` does. | `gotchas/win-83-shortpath-realpath-divergence.md` |
| `[scaffold/symlink-escape]` | `scaffoldProject` writes `.autodev/` with `mkdir`/`writeFile`, which FOLLOW symlinks — a hostile checkout with `.autodev -> /outside` (or `.autodev/queue -> /outside`) lands the skeleton OUTSIDE the repo (`existsSync`/`stat` follow links, giving a false "safe"). Fix = `lstat` `.autodev` and refuse if it's not a real dir, THEN `readdir` and refuse any symlinked direct child, BEFORE any write. `wx`/`O_EXCL` guards only the final file component, not intermediate dirs a recursive mkdir walks. | `gotchas/scaffold-symlink-escape.md` |
| `[registry/json-win-backslash]` | A hand-written `projects.json` with raw Windows `\` paths (`"D:\projects\x"`) is INVALID JSON (`\p`/`\a`/`\U` aren't legal escapes) → `JSON.parse` throws → `loadRegistry` fails soft to an EMPTY registry (+ 1 ERROR log), so the UI shows zero projects while the daemon is up. Use forward slashes (`D:/projects/x` — Node/Windows accept them); prefer `Write`/`POST /projects` over a shell heredoc (which can also collapse `\\`→`\`). Zero-projects-but-daemon-up ⇒ check stderr for `registry: corrupt` first. | `gotchas/registry-json-windows-backslash.md` |
| `[ui/light-theme-tokens]` | Light theme is a `[data-theme="light"]` override that redefines the `@theme` `--color-*` vars — works ONLY because tokens use plain `@theme` (emits `var()` refs), NOT `@theme inline` (bakes hex; would break the cascade). Only the chrome is remapped; status/verdict hues stay shared. Shared hues are fine as DOTS/tints but marginal-to-failing as TEXT on the light bg (amber/jade low-contrast on white) — if a future light surface renders a verdict tone as text, add a light-tuned darker variant, don't reuse the dark hex. | `gotchas/light-theme-tokens-and-status-hue-contrast.md` |
| `[hub/evict-on-config-write]` | A config-write endpoint that only writes `.autodev/config.yaml` to disk, without evicting the hub's cached `ProjectRoot`, leaves the LIVE daemon running the OLD gate/role config indefinitely — the write looks like it succeeded but nothing changes until a restart. Fix = `hub.evict(id)` after a successful write, wired at the composition root (keeps `admin.ts` hub-agnostic). Also documents a related codex false-positive (claimed in-flight-build race) that did NOT reproduce against the real `get()` control flow — verify a critic's claim against the actual code before patching. | `gotchas/hub-cache-must-evict-on-external-config-write.md` |
| `[scaffold/config-file-symlink]` | A config-write path must `lstat` the config FILE itself, not just its parent `.autodev` directory — a real `.autodev` dir can still contain a symlinked `config.yaml` pointing outside the repo, which `readFile`/`writeFile` would follow transparently. The existing directory-level guard (`[scaffold/symlink-escape]`) does not transfer automatically to a different write shape (single-file vs. recursive skeleton). | `gotchas/config-write-must-guard-the-file-not-just-its-parent-dir.md` |
| `[config/yaml-merge-drops-comments]` | A config-write endpoint that merges via parse→merge→`yaml.stringify` re-emits the WHOLE file, silently dropping any hand-written comments in the original `config.yaml` (confirmed live: aurora's custom header was replaced by the generic scaffold header after a single-field UI edit). Accepted tradeoff for a first-cut write endpoint (comment-preserving YAML editing is real scope, not yet needed) — but keep the UI honest that a save rewrites the file. | `gotchas/yaml-merge-write-drops-hand-written-comments.md` |
| `[worktree/vendor-junction-autoload-basedir]` | Provisioning `vendor` as a junction breaks Composer's autoloader in the worktree: PHP resolves `__DIR__` through the junction to its REAL target, so `$baseDir` → the MAIN clone, and project classes autoload from the main clone while worktree-relative `require_once` loads the worktree copy → `Cannot redeclare class` / exit 255. Read-by-path tools (`php -l`, phpcs, phpstan) are unaffected; a gate that EXECUTES project code (phpunit loading real plugins) fatals — even when identical `composer check` is green on the main tree. Rule: use a static gate (`composer check:static` = phpcs+phpstan) over a junction'd `vendor`; a runtime phpunit gate needs per-worktree vendor materialization (backlog). Found s21 ops-proof. | `gotchas/vendor-junction-composer-autoload-basedir.md` |
| `[build/stale-dist-backend]` | `npm run build:ui` compiles ONLY `ui/` → `dist/ui`; it does NOT `tsc` the daemon's `src/**`. So after a BACKEND change, a UI-only build leaves the served `node dist/index.js` STALE — a brand-new route 404s in a live browser-smoke even though its unit tests + typecheck are green (the code isn't in the running binary). Before any live smoke that exercises a backend change, rebuild BOTH (`npm run build` AND `npm run build:ui`) then restart `serve`. A new-endpoint 404 with passing tests ⇒ suspect stale dist first. Found s23. | `gotchas/stale-dist-backend-after-ui-only-build.md` |
| `[escalate/replied-holds-filelock]` | A replied escalation is NOT auto-moved out of `queue/escalated/` (`POST /escalations/:id/reply` writes `*.reply.json` but leaves the task file), and `claimNextTask` treats an escalated task's `file_set` as a lock exactly like an active one → every future run targeting the SAME file is silently blocked: decompose+enqueue succeed, "triggered" logs, but the task sits in pending, no worktree, worker never runs, conductor.log doesn't grow, `--once` exits 0 claiming nothing, no operator signal. Looks like a spawn kill but is a file-lock. Diagnose: compare `escalated/*` `file_set` vs the pending task's. Found s25 live demo. **RESOLVED s26:** reply-apply moves the task out of `escalated/` — B→`pending` (rework), A→`quarantine` (accept). A goes to quarantine NOT `done` (codex High: `done` would falsely satisfy a dependent's `depends_on` on uncommitted work, since there's no apply-on-accept machinery); quarantine releases the lock without claiming repo-completion. | `gotchas/replied-escalation-holds-filelock.md` |
| `[usage/type-strip-not-runtime-strip]` | Removing a field from a TS type does NOT strip it from a persisted runtime object: `JSON.stringify` serializes the actual runtime shape, so an artifact writer that passes an input object through by reference (`runs: workerRuns`) can leak a field the type no longer declares (e.g. a stray `total_cost_usd`) — `tsc` stays green, the leak only shows on disk. Strip at the WRITE BOUNDARY (rebuild with an explicit field pick) so "no cost in the artifact" is a structural guarantee of the writer, not a property that depends on every upstream constructor. Regression-test by injecting a stray field and asserting `JSON.stringify(doc)` has no `/cost/i`. Asymmetry: the READ guard (`isTokenUsageDoc`) deliberately IGNORES a legacy extra field (backward-compat) — tolerant-in, strict-out. Found s25. | `gotchas/type-strip-not-runtime-strip.md` |
| `[conductor/per-round-overwrite-stale]` | Persisting a per-task "latest single value" runtime artifact (e.g. `critic-verdict.json`) by writing it on EVERY retry round leaves a STALE artifact when a later round yields nothing to write: round 0 `uncertain`→persist→retry, round 1 `null`→escalate (guard skips write) ⇒ the round-0 verdict survives and contradicts the decisive outcome. The sibling `token-usage.json` copies the same write-every-round shape but is SAFE because it ACCUMULATES (monotonic). Rule: for a latest-value artifact, persist ONLY at the loop's decisive exits (clean-break commit, escalation) and only when there is a real value — never on intermediate rounds; a valueless final round then leaves no artifact (honest) rather than a stale one. Regression-test the `parseable→retry→null→escalate` sequence asserts the file is absent. Found s24. | `gotchas/per-round-overwrite-artifact-stale.md` |
| `[ui/heterogeneity-badge-forward-looking]` | The role-matrix heterogeneity warn badge (driven by `ProjectConfigView.heterogeneityWarnings` → `heterogeneityWarnings(cfg)`) fires only when worker & critic share an adapter family — but `assertKnownAdapters` (root.ts:107) forces worker∈{claude}/critic∈{codex}, whose families always differ, so a same-family config never loads and the badge CANNOT be live-demoed today. It's deliberate forward-looking insurance (lights up automatically when the adapter allowlist widens to allow a same-family pair). Prove its data path via `config-view.test.ts`, not a live serve; add a live-smoke only if `WORKER_ADAPTERS`/`CRITIC_ADAPTERS` are relaxed. Found s27. | `gotchas/heterogeneity-badge-forward-looking.md` |
| `[agents/inherit-ambient-extensions]` | The spawned worker (`claude -p`) and critic (`codex exec`) child CLIs INHERIT the operator's full ambient extension set at runtime — global `~/.claude`/`~/.codex` AND project `.claude`/`.mcp.json`/`AGENTS.md` (env passthrough + worktree cwd + `-p`/`exec` don't skip loading). Proven live s28: a bare-cwd worker loaded 9 MCP / 46 skills / 78 slash / 17 plugins / 11 agents + fired hooks. So every worker/critic run carries the operator's whole personal setup (reproducibility/surprise risk), and an "attach extensions" UI is redundant — they already see everything. Lever: per-project `isolation.worker.*` (OFF by default). Found s28. | `gotchas/spawned-agents-inherit-ambient-extensions.md` |
| `[detect/isolation-flags-not-orthogonal]` | The `claude` isolation flags are NOT cleanly orthogonal (probe C): `--strict-mcp-config` (MCP-only) and `--disable-slash-commands` (skills-only) are clean levers, but `--bare` is the full clean-room (also drops MCP + most skills; no clean "plugins+hooks only" flag exists). So model isolation as a Clean-room MASTER (`--bare`, subsumes the others → grey them) + two orthogonal toggles, NOT three independent checkboxes; `workerIsolationFlags` emits `["--bare"]` alone for cleanRoom. Also: the init `plugins` COUNT stays full even under clean-room (lists INSTALLED not ACTIVE) — never surface it as "active"; honest signals are mcp_servers/skills/slash_commands/agents. Clean-room lands at 0/14/33/3 (built-ins), not all-zero. Found s28. | `gotchas/isolation-flags-not-orthogonal.md` |
| `[onboarding/init-untracked-and-branch-autoswitch]` | New Project `init git` (`admin.initGit`) makes an EMPTY bootstrap commit and leaves the folder's existing files UNTRACKED (never `git add`) — so `mergeAfterGate` (checks `git status --porcelain`, counts `??`) refuses the FIRST merge until the operator commits a baseline; the UI hints this. Also: register + every `serve` startup AUTO-SWITCH the checked-out branch to `^autodev/` (switch existing, else create `autodev/main`; dirty tree carries over, no stash). A zero-commit (unborn-HEAD) existing repo → `currentBranch` throws (rev-parse exit 128) → register returns typed `branch_ensure_failed` (no swallow), startup pass swallows (best-effort). Don't "fix" by auto-`git add` (blind-commits node_modules/secrets). Found s30. | `gotchas/onboarding-init-git-untracked-and-branch-autoswitch.md` |
| `[detect/executable-probe]` | PATH-scan agent detection: `existsSync` is a false positive BOTH ways. (1) A bare `existsSync(join(dir,"codex"))` MISSES a Windows shim `codex.cmd`/`claude.EXE` → false "not installed" (must walk `dirs × PATHEXT`, the flip side of `[node/win-cmd-spawn]` — cross-spawn handles the SPAWN path, detection is our own separate READ probe). (2) `existsSync` is TRUE for a directory or a non-executable file of that name → false "installed"; a candidate counts only as a real FILE (`statSync().isFile()`) and, on POSIX, `X_OK` (`accessSync`). Use `path.resolve` (absolute even for a relative PATH entry); drop empty PATH segments (cwd never probed). A version-probe kill deadline must SIGTERM→(grace)→SIGKILL, not a single `kill()` (a POSIX child can ignore SIGTERM → promise hangs); keep `runNative.timeoutMs` opt-in so enforcement callers are untouched. A simulated-`win32` test must write the fixture with the SAME casing the resolver produces (`codex.CMD`) or it fails on case-sensitive CI; gate a POSIX-only `X_OK` test with `it.skipIf(win32)`. Found s26. | `gotchas/detect-executable-probe.md` |
| `[env/serena-churn-blocks-merge]` | A git-TRACKED `.serena/project.yml` (+ `.serena/memories/*`) is auto-rewritten by the Serena MCP language server in the background → the main working tree goes dirty DURING a run → `mergeAfterGate` refuses (`git status --porcelain` non-empty) → every task escalates `blocked` and NONE reaches DONE, even right after a verified-clean `reset --hard`+`clean`. Same class as `.autodev` churn (`[conductor/real-repo-run]`) but `.serena` wasn't excluded. Fix now: `git checkout -- .serena` + `git ls-files .serena \| xargs -I{} git update-index --skip-worktree {}`. Fix product: onboarding should git-exclude `.serena/`+`.autodev/`. Also: an untracked Windows reserved name `nul` can't be removed by `git clean` (Permission denied) — delete via `\\?\` long-path prefix. Found s31. | `gotchas/serena-churn-blocks-merge.md` |
| `[api/run-id-dot-validation-mismatch]` | Run ids are generated by `slugifyIntent` which KEEPS `.` (intent naming `OVERVIEW.md` → `run-…-overview.md-…`), re-validated on WRITE by `isPathSafeId` (allows `.`, blocks `..`). But the READ side (`isRunManifest` listing + `/runs/:id[/usage]` + PATCH routes) used the stricter dot-free `safeIdSegment` → every filename-derived run failed read-validation → `GET /runs` returned `[]`, dashboard showed "No runs yet" (mislabelled in logs as "unreadable/invalid run manifest" — the manifests were fine). Fix (`a1c81d2`): reuse `isPathSafeId` as the run-id read validator (`safeRunId`); task/escalation ids stay strict. Lesson: a value produced by one allowlist and consumed by another must use the SAME function — a stricter reader silently drops legit data with no actionable error. Found s31. | `gotchas/run-id-dot-validation-mismatch.md` |
| `[orchestrator/llm-retitle-breaks-task-level-dedup]` | A relaunch-dedup heuristic keyed on file_set-overlap AND normalized-title-match passed 6 unit tests (built with matched titles) but MISSED a real relaunch live: opus re-titles identical work on every decompose, so a genuine relaunch's second task shares `file_set` but not `title` → fail-open → duplicate enqueued. Fix: dedup on the OPERATOR's intent TEXT (stable across decompositions) via a new `caps.read.recentRuns()`, checked BEFORE the expensive decompose; task-level kept as a secondary layer. Lesson: don't test a dedup/comparison heuristic with matched fixtures when one side is LLM output — the fixture can't reproduce the drift that breaks it in production; live-prove features touching LLM-generated identity. Found s32. | `gotchas/llm-retitle-breaks-task-level-dedup.md` |
| `[ui/shadcn-scaffold-assumes-unmounted-next-themes]` | The shadcn `sonner` toast primitive (`ui/components/ui/sonner.tsx`) called `useTheme()` from `next-themes` — a library whose `<ThemeProvider>` is never mounted anywhere in this app (own theme system: `@/lib/theme`, a `.dark` class on `<html>`). Silently sat broken (theme would never track the operator's real choice) since the shadcn migration, unnoticed because the component was never actually rendered until s32 wired it up. Fix: swap to the project's real `@/lib/theme` hook. Lesson: an unused shadcn-scaffolded primitive compiling is not the same as it being wired correctly — audit its boilerplate hooks (theme/router/etc.) against what's ACTUALLY mounted at `main.tsx` before first use. Found s32. | `gotchas/shadcn-scaffold-assumes-unmounted-next-themes.md` |
| `[ui/fire-and-forget-action-needs-feedback-at-point-of-action]` | `POST /orchestrate` is fire-and-forget (202 immediately); its real outcome (enqueued / relaunch-dedup skip / 0-task / rejected) is reported ONLY as a digest.md line, and the only UI surface for that (`DigestStrip`) lives on `RunView`, a DIFFERENT page reached by clicking into a run — but a dedup-skip creates no new run to click into. The composer's static "Run accepted…" message never updated, so a legitimate no-op looked exactly like a silent failure (operator-flagged live, s32). Fix (PR #60): toast the digest's first new outcome line on the SAME page the action was taken from, reusing the already-WS-live `digestTail`. Lesson: for any fire-and-forget action, ask at design time "if this is a legitimate no-op, how does the operator find out from the exact screen where they clicked" — a feedback channel reachable only via a side-effect the no-op path doesn't produce is a silent failure in practice. Found s32. | `gotchas/fire-and-forget-action-needs-feedback-at-point-of-action.md` |
| `[cli/claude-stream-json-multiturn]` | `claude -p --input-format stream-json --output-format stream-json --replay-user-messages --verbose` starts ONE process that accepts MULTIPLE sequential user turns over stdin, keeping the same `session_id` throughout — live-verified (not inferred). `--print`+`--output-format=stream-json` REQUIRES `--verbose` or it errors immediately. `--bare` mode strictly requires an explicit `ANTHROPIC_API_KEY` (OAuth/keychain never read) — a spawn that works unflagged can fail "Not logged in" under `--bare`. The process only exits when stdin is closed/killed. Found s33 (orchestrator-chat design). | `gotchas/claude-cli-stream-json-multiturn.md` |
| `[chat/onToken-bound-once]` | `ClaudeChatProcess` binds its `onToken` callback ONCE at construction and reuses it for EVERY turn over a chat session's whole lifetime (one long-lived process, not a fresh spawn per turn) — so a per-request "no SSE client yet" no-op passed at session-start silently served every LATER turn too, and live token streaming never worked past the opening turn despite `ChatSessionManager`'s `attachStream()`/`sseRes` machinery being fully correct and unit-tested in isolation. Fix: the bound closure must look up the CURRENTLY attached sink fresh on every call (`this.sessions.get(liveSessionId)?.sseRes`), not capture a value at bind time. Also: `res.writeHead()` alone doesn't flush SSE headers — call `res.flushHeaders()` or a client sees nothing until the first token. Found live (codex review + a real `curl -N` SSE capture), s34. | `gotchas/chat-onToken-bound-once-per-process-lifetime.md` |
| `[ui/base-nova-ports-catalog-to-base-ui]` | Our `components.json` style is **`base-nova`** (shadcn on **Base UI**). The shadcn MCP's item metadata reports the **default-style (Radix)** deps, so `bubble`/`item`/`sidebar`/`collapsible`/`tabs`/`toggle-group`/`checkbox`/`alert-dialog` all appear to pull `radix-ui` → reads as "foundation mismatch, keep hand-rolled." **Wrong:** `base-nova` ships **Base-UI ports** of the whole catalog (verified by fetching `ui.shadcn.com/r/styles/base-nova/<item>.json` — every one imports `@base-ui/react/*`, none pull Radix). So there is NO radix-foundation trap; the real adoption filter is value/churn/behaviour, not radix-vs-base-ui. Don't trust MCP default-style dep metadata for the foundation call — fetch the style-specific JSON. Brief audit subagents with this or they'll wrongly tag half the catalog FOUNDATION-COST. Found s35. | `gotchas/shadcn-base-nova-ports-whole-catalog-to-base-ui.md` |
| `[ui/shadcn-cli-vendor-windows]` | `npx shadcn add <component>` can't be driven non-interactively on this box: it (re)writes the component's `registryDependencies` (e.g. `button`), and Windows' case-insensitive FS makes its stock `button.tsx` MATCH this project's custom `Button.tsx` → an overwrite prompt that `--yes`/closed-stdin don't bypass (would clobber the custom Button). Workaround: `curl` the registry item JSON (`ui.shadcn.com/r/styles/<style>/<component>.json`), write only `files[0].content` to `ui/src/components/ui/`, `npm install` its declared dep, then hand-rewrite the site-internal aliases the CLI would have (→ `@/lib/utils`, `@/components/ui/Button`, lucide instead of `IconPlaceholder`, and map the component's Button variant/size to this project's custom cva API). Real fix = the shadcn MCP (wired into `.mcp.json` s34, live next session). Found s34. | `gotchas/shadcn-cli-vendoring-on-windows.md` |
| `[ui/twmerge-important-arbitrary-variant-no-dedupe]` | tailwind-merge does NOT dedupe two utilities that share an `!important` marker AND an arbitrary variant (e.g. `group-data-[collapsible=icon]:p-2!` vs `:p-0!`) — it keeps both, so **stylesheet order** (not class order) picks the winner. Bit the shadcn `sidebar` block: `SidebarMenuButton size="lg"` never dropped to `p-0` when collapsed → stayed `p-2` → 16px content box → a `size-8` icon-square overflowed to a clipped vertical sliver, off-center. Fix: don't rely on the fragile `p-0`; use a bare `size-4` icon (centers in the 16px `p-2` box, like the block's default buttons) — NOT a `group-data-[collapsible=icon]:hidden`/`:size-4` crutch. When an expected Tailwind override silently doesn't apply, MEASURE the computed style; a still-applied "losing" class ⇒ suspect a non-dedupe. Found s36. | `gotchas/tailwind-merge-important-arbitrary-variant-no-dedupe.md` |
| `[ui/base-ui-checkbox-wrapping-label]` | A Base UI `Checkbox.Root` renders a `<span>` + hidden input by default, so a SIBLING `<label htmlFor="id">` does NOT toggle it (a label only activates a labelable element, not a span) — clicking the descriptive text does nothing, and the text may not become the accessible label. Fix: WRAP the `<Checkbox>` AND the text in ONE `<label>` (clicking anywhere forwards to the hidden input) — the classic pattern, and what the hand-rolled original did; or make the root a native button (`nativeButton render={<button/>}`) if you need the sibling form. Don't assume shadcn's "sibling label" examples apply to every primitive. Also: `onCheckedChange={setStateFn}` is safe — Base UI passes `(boolean, eventDetails)` and a 1-arg React setter ignores the 2nd arg. Found s36 (codex-caught). | `gotchas/base-ui-checkbox-needs-wrapping-label.md` |
| `[gate/agent-ci-not-runnable-on-native-windows]` | `@redwoodjs/agent-ci@0.16.2` CANNOT run on native Windows — it dies BEFORE Docker at `tar -czf C:\...` (its cache layer shells to a POSIX `tar`; the Unix/MSYS tar on PATH reads `C:\...` as a remote host → `Cannot connect to C: resolve failed`). So the `gate.agentCi` feature is Linux/WSL-only in practice: on a Windows box every task ESCALATES with `gate threw -- broken operator config` (the module gets no parseable `run.finish` → throws → conductor escalates) — which is the CORRECT fail-safe (unfixable env → escalate, never loop the worker), live-proven for real s37. To actually USE or live-prove agent-ci (pass/job-fail branches) run under WSL/Linux (Docker Desktop WSL integration exposes the same daemon). Don't shim agent-ci's tar — it's agent-ci's own vendored-dep bug, not ours. Found s37. | `gotchas/agent-ci-not-runnable-on-native-windows.md` |
| `[gate/agent-ci-worktree-wsl-git-interop]` | agent-ci run in the per-task **Windows-created git worktree under WSL** hits TWO git-interop traps a live-prove (worker→gate→**merge**) found, both invisible to unit tests. **(1)** The worktree `.git` file's gitdir pointer is a Windows path (`gitdir: D:/...`); WSL git can't follow it → `git rev-parse HEAD` fails → agent-ci emits only `run.start` → gate reads infra → escalates on EVERY real run. Fix: derive the WSL gitdir (`worktreeGitDirWsl` → `winToWslPath`) and set **`GIT_DIR` only** for the WSL spawn. **(2)** agent-ci MUTATES the shared `.git/config` (via `GIT_DIR`→ the linked worktree's config IS the main repo's): `GIT_WORK_TREE` persists `core.worktree=/mnt/...` (→ Windows conductor `fatal: Invalid path '/mnt'`), and agent-ci flips `core.bare=true` (→ main tree "bare" → post-gate merge `fatal: must be run in a work tree`). Fix: **never set `GIT_WORK_TREE`** (rev-parse needs only `GIT_DIR`) + **snapshot/restore `<repoRoot>/.git/config`** around the run (conductor is single-threaded, restore on throw too). Lesson: a tool that "runs your CI" can corrupt the repo you point it at; isolate it + live-prove the whole flow through the merge. Found s38. | `gotchas/agent-ci-worktree-wsl-git-interop.md` |
| `[gate/agent-ci-ndjson-keyed-by-event-not-type]` | agent-ci's `--json` NDJSON keys every line by **`event`, NOT `type`** (terminal: `{"event":"run.finish","status":"passed"\|"failed"}`; verified live). The parser was first written defensively against a GUESSED `type` key — all unit tests (using the guessed shape) were green, but keyed on `type` EVERY real run (pass or fail) would parse as infra → throw → escalate: green tests, 100% useless in production (can only ever escalate). Fix: `kind = obj["event"] ?? obj["type"]` (`type` a defensive fallback); `status` verdict was already handled. Lock the real shape with a VERBATIM real-NDJSON fixture. Lesson: never ship a parser for an external tool's/LLM's wire format on a guessed shape — self-authored fixtures are vacuously green (same class as `[orchestrator/llm-retitle-breaks-task-level-dedup]`); capture the real output once and pin it. This is why a live-prove is mandatory even for an off-by-default feature. Found s37. | `gotchas/agent-ci-ndjson-keyed-by-event-not-type.md` |

| `[chat/launch-marker-needs-prompt-contract]` | A control marker the backend DETECTS (`[[LAUNCH]]` for launch-by-word) is DEAD unless the LLM PROMPT teaches the model to EMIT it. s40: backend detection + guards were built and unit-tested, but `chat-prompt.ts` never told the model the marker exists (it even said "cannot trigger a run yourself") → the model narrated but never sent `[[LAUNCH]]` → launch-by-word silently never fired, every unit test green, the live run stayed `chatting`. A feature split across DETECT (code) + EMIT (prompt) must add BOTH in one change set + a test pinning the prompt contract, and be live-proven with a real model (self-authored detect-side fixtures prove nothing about emission — same class as `[orchestrator/llm-retitle-breaks-task-level-dedup]`). Found s40. | `gotchas/launch-marker-needs-prompt-contract.md` |
| `[ui/dotted-id-breaks-spa-reload]` | A URL path segment derived from free text can contain a dot (`slugifyIntent` keeps dots → a thread id like `...FAQ.md...` from an intent naming a file). Clicking a link INSIDE the app works (TanStack client routing), but a RELOAD / direct-nav of `/p/:id/t/<dotted-id>` hits the static server, whose SPA-fallback treats the dotted last segment as a FILE → returns `{"error":"not found"}` instead of `index.html` (confirmed: dotted → 404, dot-free → 200 text/html). s40 fix (contained): `mintThreadId` strips dots. Root cause still open (backlog): run ids + task ids also keep dots → same reload-404; proper fix = extension-agnostic SPA-fallback in the static handler (touches the security-sensitive `[api/static-traversal]` path). Rule: any free-text-derived URL path segment must be dot-free OR the fallback extension-agnostic; TEST A RELOAD, not just an in-app click. Found s40. | `gotchas/dotted-id-breaks-spa-reload.md` |
| `[rework/reply-b-drops-critic-feedback]` | Escalation reply **B** (reject & require rework) resets the task `escalated/→pending/` but the worker never gets the critic's objection on the re-run, so it reproduces the same diff and re-escalates. Two compounding gaps in `conductor.ts`: (1) the immediate-escalation branch (`contractRisk`, incl. any non-empty `broken_contracts`) persists `critic-verdict.json` but NOT `critic-feedback.md`; (2) a re-claimed pending task starts at `round 0`, and `criticFeedback` is read ONLY when `round > 0`. So reply-B rework is non-functional for the common contract-risk escalation. (Also: reply-B doesn't auto-trigger a drain — the task sits inert until an unrelated trigger.) Fix: always persist verdict notes to `critic-feedback.md` on escalation + feed it at round 0 on re-claim. Found s41. **RESOLVED s42** (fix commit `3fd977b`): (a) escalation branch now writes `critic-feedback.md`; (b) round-0 read is unconditional (a fresh claim has no file → undefined); (c) reply-B fires a best-effort `onReplyRework`→`conductor.run({drain})` so the reworked task actually runs. Proven by 3 conductor + 5 server unit tests + a real repo+scheduler integration test (feedback survives escalated→pending→re-claim) + live daemon boot-smoke. | `gotchas/reply-b-rework-drops-critic-feedback.md` |
| `[gate/critic-before-ci-blocks-testless-repos]` | The `agent-ci` CI step is downstream of a critic-**clean** verdict (the round loop only `break`s to `runGate` on clean), and the critic prompt (`src/critic/prompt.ts:46,65`) HARD-demands a guard/test for any behavioral change ("missing guard" → not clean; not tunable via `effort`). So in a repo with no reachable test infra (e.g. a WC plugin whose phpunit needs wp-env), EVERY feature task escalates before CI: proven live s41 — a `WC_Integration` and even a trivial correct getter both `broken` on "no coverage"; only a behavior-neutral **docblock** change passed the critic → reached the gate → agent-ci green → DONE (`3609a2c`). Open product question: run CI as an earlier/independent step, or soften the critic's coverage demand when there's no test harness. Found s41. **RESOLVED s42** (ADR-005, commit `21c8019`): narrowed the critic from a coverage gate to a correctness+fabrication gate — "missing guard/test" is no longer a clean-blocker; coverage is exclusively the mechanical gate's job (zones+guards) + agent-ci. Kept CI where it is (rejected reordering). Proven end-to-end on the REAL codex critic: correct getter → clean 0.82 (was broken 0.73 in s41); real load-order bug → broken 0.76 (gate stays real). | `gotchas/critic-before-ci-blocks-testless-repos.md` |
| `[gate/agent-ci-needs-github-remote-slug]` | `agent-ci run` refuses to start unless it resolves a GitHub `owner/repo` slug — from a git remote OR `GITHUB_REPO`. The harness (`buildAgentCiCommand`) sets neither, so it relies on the target repo having a GitHub `origin` (a per-task worktree shares the main repo's remotes → works if the clone has one). A remote-less/non-GitHub repo → immediate `Could not detect GitHub repository` → no `run.finish` → infra escalate on every task. Fix: inject a synthetic `GITHUB_REPO=local/<repoName>` when no GitHub origin exists. Found s41. | `gotchas/agent-ci-needs-github-remote-slug.md` |
| `[gate/agent-ci-workflow-container-no-checkout]` | For a workflow to actually RUN under agent-ci, shape it as a `container:` job with tools pre-installed and **NO `actions/checkout`**. Tested s41: `ubuntu-latest + setup-php` → setup-php FAILS (can't download PHP); `container: php:8.3-cli + checkout` → checkout FAILS (no git/node in the image); **`container: php:8.3-cli` + no checkout + `php -l`** → PASSES (agent-ci mounts the workspace). So the gate's `ci.yml` is optimized for agent-ci, not dual-purpose GitHub CI (a checkout-less container job is vacuous on real GitHub). Found s41. | `gotchas/agent-ci-workflow-container-no-checkout.md` |
| `[narrator/escalated-run-not-terminal]` | `NarratorService` treats a run as finished (sets thread `status:done/error`, `stop()`s its interval + CI subs) only when EVERY task is terminal = `done`\|`quarantine`. An **escalated** task is NOT terminal (parked awaiting operator A/B) → the thread stays `status:running` forever and the narrator idle-ticks every 1.5s doing nothing until daemon stop. Not a correctness bug (the escalation cell + narration DO surface it; R1/gate untouched) — a UX + resource gap: no "blocked on operator" status, wasteful idle loop. s41 chat-polish fix: treat all-terminal-or-escalated (none active/pending) as a stop → a distinct `blocked` status + final "waiting on your reply" narration + `stop()`; re-arm on escalation reply (`[escalate/replied-holds-filelock]`). Found s40 (two live escalations). **RESOLVED s43** (feat `3919377` + `0d78b52`): a run with no `active`/`pending` task but ≥1 `escalated` now parks a distinct `blocked` status + a one-shot "waiting on your reply" note + `stop()` (no more idle-tick); `onReplyRework(taskId)` re-arms a fresh narrator bound to the same run (`boundRunId`, silent baseline → narrates only post-reply progress). codex gpt-5.5 gate `uncertain` (2 race findings verified false against server.ts move-before-hook ordering; 1 observability closed by an INFO re-arm log). **Live-proven end-to-end**: thread → BLOCKED badge + amber PauseCircle glyph + note + narrator-stopped → reply-B → `running` re-arm (INFO log) → terminal handled (`error`, no wedge). | `gotchas/escalated-run-not-terminal.md` |
| `[critic/codex]` | The `codex:codex-rescue` subagent's INLINE-embedded diff (the Windows `codex exec` workaround) can STRIP string-literal quotes + the `+` prefix in transit, so codex sees bare fragments (`uncovered,,`, `choice === B`) and reports a FALSE `blocker`: "syntactically invalid / won't compile". Hit TWICE in s42 (critic-prompt + reply-B reviews), both false. The doubled comma is the tell. Verify against ground truth (`typecheck`/`build`/tests green + the file ran) and DECLINE — never patch a codex syntax-blocker without Reading the actual lines. codex's SUBSTANTIVE findings in the same review are unaffected (s42's real `major` on an unguarded hook was genuine). Found s42. | `gotchas/codex-inline-diff-strips-quotes-false-blocker.md` |
| `[critic/gpt-5.6-variant-behaviors]` | The three gpt-5.6 critic variants have stable, distinct failure modes (s44 calibration, 4 cases × 3 rounds): **sol** deterministically FALSE-BLOCKS a correct change (worst gate profile, most expensive; and it's the CLI DEFAULT — un-pinned gates drift onto it); **terra** unreliably catches a real bug (~1-in-3 miss = "merge bullshit"); **luna** 12/12 correct, matches the trusted 5.5 baseline, cheapest → **promoted to critic default**. "Newer/pricier" ≠ "better critic" — calibrate ≥3 rounds, validate fixtures against 5.5 first, always PIN `roles.critic.model` explicitly. | `gotchas/gpt-5-6-critic-variants-sol-blocks-terra-misses.md` |
| `[rework/reply-b-poisons-maxrounds-exhausted-task]` | Reply **B** (rework) on an escalation raised by **round/attempt exhaustion** (worker failed the critic `maxRounds` times) does NOT re-run the worker — the drain re-claims the task and the conductor's poison-pill guard **immediately re-escalates it as `poison` → `quarantine`** (terminal) with no worktree/worker (~80ms). Live-proven s43 (`pickup-cart-handling-fee`: escalated after rounds 0/1/2 → reply-B → `poison` in 80ms → thread `error`). Contrast s42 `dhl-express-cart-fee`: single-round `disagreement` escalation → budget NOT exhausted → reply-B genuinely re-ran + converged. So reply-B rework works for a single-round/immediate escalation but is a no-op-then-poison for a round-exhausted one (attempt counter not reset on reply-B). Conductor attempt-budget behaviour, NOT a blocked-state defect. Candidate fix (deferred): reset the round/attempt budget on an explicit operator reply-B, or surface the poison honestly instead of a silent terminal. Found s43. **RESOLVED s44**: the reply-B (choice-B) branch resets the attempt budget (`setAttempts(id, 0)`) after the escalated→pending move and before the drain, so the re-claim increments 0→1 and reaches the worker (not poison). Scoped to B + a real move; best-effort; the in-loop retry poison-pill is unchanged. 2 unit + 1 real repo+scheduler integration test; gpt-5.6-luna-gated + live-proven. | `gotchas/reply-b-poisons-maxrounds-exhausted-task.md` |

| `[autonomy/budget-saga-order]` | A per-task budget spanning TWO blackboard files (the `auto-rework-count` runtime file + the `escalated→pending` requeue move) can't be atomic; every single-direction order fails a different way (journal-before → false journal; requeue-first → cross-run over-budget on persistent counter-write failure; counter-first → false-park on requeue failure). Fix = a SAGA: persist the budget FIRST (a persistent write failure throws before any requeue → no over-budget), requeue in a try + COMPENSATE (roll the counter back) on failure → retry cleanly (no false-park), rethrow; advance an in-memory `seen` tally (`max(persisted,seen)`) for termination independent of the file layer; journal LAST (a completed action). Rollback `.catch` emits a guarded WARN (not silent). Pick the safe fail-direction (LESS unattended spend) since no ordering is atomic. Took 4 codex gpt-5.6-luna passes to converge (s45). | `gotchas/budget-saga-order-on-file-blackboard.md` |
| `[conductor/once-precedes-drain]` | `conductor.run` breaks on `once` (`conductor.ts:705`) BEFORE it ever evaluates `drain` (`:719`), so `{once:true, drain:true}` runs exactly ONE iteration, silently — the flags read as independent bounds but `once` is absolute. Bites when opts are INHERITED, not authored (the overnight supervisor shares `runOrSupervise` with the `once:true` CLI path). Fix = strip the flag at the boundary that changes the run's meaning (`supervisorRunOpts()` drops `once`), never by reordering the conductor. Sibling half: a "bounded default" written `opts ?? {once:true}` guards only `undefined` — `trigger({})` is unbounded; and `Object.keys(opts).length > 0` is no better, since `{once:false, drain:false}` has keys and no bound. Guard spend with an allow-list over VALUES (`hasBound()`), not key presence — and derive the predicate from how the value is CONSUMED: `typeof maxIterations === "number"` still admits `NaN`/`Infinity`, for which the conductor's `iterations >= maxIterations` never fires, so use `Number.isFinite` (`0`/negatives ARE bounded there — don't reject them). Caught by the codex gate across 4 rounds, never by tests or typecheck, with rounds 2-4 each finding a narrower leak in the previous round's FIX (`??` → key-count → `typeof number` → `isFinite`). Found s46. | `gotchas/conductor-once-precedes-drain-and-bounded-defaults.md` |

## Anticipated tag namespaces

- `[fork/*]` — fork-hygiene / upstream-merge pitfalls
- `[ao/*]` — AO daemon/CLI behaviours
- `[electron/*]` — AO desktop UI internals
- `[critic/*]` — critic-gate wiring pitfalls
